It seems that searching for a DIP pair with LDB < threshold helps with the search performance, initial tests show that in various formula the threshold changes result in substantial performance improvements/worsening.

The intuition now is that the dip method greatly helps with the search space but wastes a lot of time with the overhead generated by the dip search, BUT! I think i can skip it when first inspecting the history.